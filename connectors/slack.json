{
  "name": "slack",
  "required": true,
  "auth_env": ["SLACK_BOT_TOKEN"],
  "reads": ["approval_responses", "human_review_requests"],
  "writes": ["approval_requests", "exception_updates", "status_summaries"],
  "notes": "Primary team coordination channel for approvals and exception handling."
}
